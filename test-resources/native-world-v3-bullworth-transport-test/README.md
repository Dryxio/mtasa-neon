# Native world v3 Bullworth transport test

This resource publishes the canonical Bullworth format-3 child pack without
requesting startup activation. Its generated `native/` payload is runtime-only
and must match the content identity referenced by
`native-world-v3-set-test`.

The aggregate coordinator is the only resource allowed to request the
format-3 startup authorization.
